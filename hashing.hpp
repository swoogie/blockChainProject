#pragma once

#include <iostream>
#include <chrono>
#include <string>

using std::stringstream;
using std::ifstream;
using std::string;
using std::cout;
using std::cin;

using std::string;

string hashFun(string str);